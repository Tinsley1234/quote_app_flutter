import 'package:flutter/material.dart';

class ActingPage extends StatelessWidget {
  const ActingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}